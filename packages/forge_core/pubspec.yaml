name: forge_core
description: >-
  Platform-agnostic domain primitives for the Flutter Forge Kit — Result,
  DomainError, FetchPolicy, Request, UseCase, analytics, and logging. Pure Dart,
  zero Flutter and zero runtime dependencies.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"

resolution: workspace

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
