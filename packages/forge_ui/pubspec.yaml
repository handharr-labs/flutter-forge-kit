name: forge_ui
description: >-
  Token-first design system for the Flutter Forge Kit. FUI*-prefixed widgets
  driven by *Configuration objects. Standalone — holds no domain or feature
  logic. Single base tier today (future Bronze/Silver/Gold).
version: 0.2.0
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.27.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
